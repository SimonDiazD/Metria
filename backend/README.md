# Backend

Inicializa aquí tu API (Node/Express, Python/FastAPI, etc.).

Si usas Node, tu `package.json` debe tener los scripts `lint` y `test`
para que el CI funcione. Si usas Python, asegúrate de tener `requirements.txt`.
