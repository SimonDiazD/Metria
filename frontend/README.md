# Frontend

Inicializa aquí tu app (ej: `npm create vite@latest .` o `npx create-next-app .`).

Debe generar un `package.json` con al menos estos scripts para que el CI funcione:
- `lint`
- `test`
- `build`
